package org.example;

import java.io.IOException;
import java.security.GeneralSecurityException;
import java.time.LocalDateTime;
import java.util.Scanner;

import com.github.javafaker.Faker;
import org.example.Utils.*;
import org.example.Bots_dev.*;

import static org.example.Bots_dev.ChromeBot.browse;

public class Main {
    public static void main(String[] args) throws InterruptedException, GeneralSecurityException, IOException {
        /*

        System.out.println("Started.");
        showNav();
        Scanner in = new Scanner(System.in);
        int code = in.nextInt();

        while (code != 0) {
            switch (code) {
                case 1:
                    DiscordBot.startBot();
                    break;
                case 2:
                    GoogleSearchBot.startBot();
                    break;
                case 3:
                    OneDriveBot.startBot();
                    break;
                case 4:
                    YoutubeBot.vlc();
                    break;
                case 5:
                    TwitchBot.startBot();
                    break;
            }
            showNav();
            code = in.nextInt();
        }

        System.out.println("Exiting...");
    }

    public static void showNav() {
        System.out.println("Please specify the test to run:");
        System.out.println("1 for Discord Test");
        System.out.println("2 for Google Search Test");
        System.out.println("3 for OneDrive Test");
        System.out.println("4 for Youtube Test");
        System.out.println("5 for Twitch Test");

        System.out.println("0 to exit");

         */

        /*
        util.replaceLine("tshark.sh","tshark -a duration:3600 -w ~/" + "2.pcapng");
        logParsers.parsePcap("~/scap.pcapng");
        logParsers.parsePcap("~/mcap.pcapng");
        logParsers.parsePcap("~/lcap.pcapng");

        Faker faker = new Faker();
        System.out.println(faker.lorem().fixedString(100));

         */
    }
}
